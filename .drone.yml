kind: pipeline
type: kubernetes
name: default

steps:
- name: build-image
  image: plugins/docker
  settings:
    registry: registry.zanelab.com
    repo: registry.zanelab.com/zhala/site
    tags: latest